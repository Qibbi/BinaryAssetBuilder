namespace SageBinaryData
{
    public enum VeterancyLevel
    {
        INVALID = -1,
        REGULAR,
        VETERAN,
        ELITE,
        HEROIC
    }

    public enum CommandSourceType
    {
        FROM_PLAYER,
        FROM_SCRIPT,
        FROM_AI,
        FROM_DOZER
    }
}
